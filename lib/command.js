// Generated by CoffeeScript 1.6.3
(function() {
  var doctest, options, path, program, _, _i, _len, _ref;

  program = require('commander');

  _ = require('underscore');

  doctest = require('../lib/doctest');

  program.version(doctest.version).usage('[options] path/to/js/or/coffee/module').option('-m, --module [type]', 'specify module system ("amd" or "commonjs")').parse(process.argv);

  options = _.pick(program, ['module']);

  _ref = program.args;
  for (_i = 0, _len = _ref.length; _i < _len; _i++) {
    path = _ref[_i];
    doctest(path, options);
  }

}).call(this);
